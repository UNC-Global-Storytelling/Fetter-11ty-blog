<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ pathPrefix }}styles.css">
</head>
<body>
    <nav>
        <a href="{{ pathPrefix }}">My Blog</a>
        <a href="{{ pathPrefix }}about/">About</a>
        <div class="dropdown">
            <button class="dropbtn">Posts 
                <i class="fa fa-caret-down"></i>
            </button>
            <div class="dropdown-content">
                {% for post in collections.posts %}
                <a href="{{ pathPrefix }}{{ post.url }}">{{ post.data.title }}</a>
                {% endfor %}
            </div>
        </div>
    </nav>
    <div class="breadcrumb">
        <a href="{{ pathPrefix }}">Home</a>
        {% if page.url contains '/posts/' %}
            > <a href="{{ pathPrefix }}posts/">Posts</a>
            {% if page.url != '/posts/' %}
                > <span>{{ title }}</span>
            {% endif %}
        {% elseif page.url contains '/about/' %}
            > <span>About</span>
        {% endif %}
    </div>
    <main>
        {{ content | safe }}
    </main>
</body>
</html>